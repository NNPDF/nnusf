# A Github workflow that runs the tests
name: Run unit tests

on:
  push:
  workflow_dispatch:

jobs:
  py39:
    uses: N3PDF/workflows/.github/workflows/python-poetry-tests.yml@allow-container
    with:
      python-version: "3.9"
      container-image: "docker://ghcr.io/n3pdf/lhapdf:0.1.1"
    secrets:
      codecov-token: ${{ secrets.CODECOV_TOKEN }}
